// Shared constants for the quiz feature

export const QUIZ_DATA_PREFIX = 'QUIZ_DATA::';

// Regex pattern to match QUIZ_DATA::quizId::jsonData format
export const QUIZ_DATA_REGEX = /QUIZ_DATA::([^:]+)::({[\s\S]*})/;
